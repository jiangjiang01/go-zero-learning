syntax = "proto3";

package userrpc;
option go_package="./userrpc";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service UserRpc {
  rpc Ping(Request) returns(Response);
}
